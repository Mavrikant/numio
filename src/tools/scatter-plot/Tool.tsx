/**
 * Scatter Plot tool — STUB. Will be filled in by the scatter-plot agent.
 * Renders "coming soon" so the dev server can compile.
 */
import type { Locale } from "@/config/site";

export default function ScatterTool({ locale: _locale }: { readonly locale: Locale }) {
  return (
    <div className="rounded-md border border-dashed border-slate-300 bg-slate-50 px-4 py-12 text-center text-sm text-slate-500 dark:border-slate-600 dark:bg-slate-800/30 dark:text-slate-400">
      Scatter plot tool — coming soon.
    </div>
  );
}
